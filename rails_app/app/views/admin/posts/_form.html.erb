<%= form_with model: [:admin, @post], local: true do |f| %>
  <div>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :body %><br>
    <%= f.rich_text_area :body %>
  </div>

  <%= f.submit %>
<% end %>
