

<%= form_with scope: :contact_form, url: contact_form_index_path,method: :post, local: true, class: "container" do |f| %>
    <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', required: true %>
    </div>

    <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, class: 'form-control', required: true %>
    </div>  

    <div class="form-group">
        <%= f.label :message %>
        <%= f.text_field :message, class: 'form-control', required: true %>
    </div> 

    <%= f.submit "Wyślij", class: "btn btn-primary" %>
<% end %>
